import 'package:flutter/material.dart';

class CreateTransportOrderScreen extends StatelessWidget {
  const CreateTransportOrderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create Transport Order Screen'),
      ),
    );
  }
}
